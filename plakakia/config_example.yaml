{
    "input_extension_images": "hdf5",
    "output_extension_images": "hdf5",
    "dataset_name" : "Hypercube", # if the ext. is 'hdf5': name of the dataset for the image/hypercube
    "tile_size": 400, # tile size in pixels
    "step_size": 380, # step size in pixels for sliding window
    "input_dir_images": "C:/Users/u0140154/OneDrive - KU Leuven/data/HSI/processed data/2021/hdf5files",
    "input_dir_annotations": "C:/Users/u0140154/OneDrive - KU Leuven/data/HSI/processed data/2021/hdf5files",
    "input_format_annotations": "pascal_voc", # yolo, pascal_voc, coco or segmentation
    "output_dir_images": "output/images",
    "output_dir_annotations": "output/annotations",
    "output_format_annotations": "pascal_voc", # yolo or pascal_voc
    "annotation_mapping": { # mapping of annotation classes to integers
        "kernel": 0,
        "person": 1,
        "bottle": 2,
        "empty": 3,
        "trafficlight": 4,
        "speedlimit": 5,
        "crosswalk": 6,
        "stop": 7
    },
    "validate_settings": true, # validate settings before running
    "log": false, # log to file
    "log_folder": "logs", # folder to save logs
    "num_workers": -1, # number of workers for multiprocessing
    "clear_duplicates": false, # clear duplicate images in the output folder
    "draw_boxes": false, # draw bounding boxes on the output images
}
